# OpenAPI

O contrato HTTP versionável do backend fica neste diretório. A especificação atual descreve somente o health check operacional existente; endpoints de produto serão adicionados apenas junto de requisitos e implementação próprios.

Valide semanticamente o documento OpenAPI 3.1 com a versão fixada do Redocly CLI:

```bash
make contract-check
```
